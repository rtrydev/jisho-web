import { AppendHintsPipe } from './append-hints.pipe';

describe('AppendHintsPipe', () => {
  it('create an instance', () => {
    const pipe = new AppendHintsPipe();
    expect(pipe).toBeTruthy();
  });
});
